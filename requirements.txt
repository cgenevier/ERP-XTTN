# Direct dependencies only. Transitive deps are resolved by pip.
# The --extra-index-url below fetches the CUDA 12.8 torch wheel to match the
# RunPod RTX PRO 6000 environment. For CPU-only installs, remove that line and
# the torch pin (+cu128 suffix); pip will fall back to PyPI's CPU wheel.
#
# For step 2 (raw → BIDS conversion) only, additionally:
#   pip install mne-bids pybv

--extra-index-url https://download.pytorch.org/whl/cu128

numpy==2.1.2
pandas==3.0.2
mne==1.12.0
matplotlib==3.10.8
scipy==1.17.1
scikit-learn==1.8.0
torch==2.8.0+cu128
pyriemann==0.11
